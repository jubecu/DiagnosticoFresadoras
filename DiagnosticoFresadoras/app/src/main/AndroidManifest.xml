<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ubu.tfg.diagnosticofresadoras">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".InfoAlarmaActivity" />
        <activity android:name=".QuestionsActivity" />
        <activity android:name=".MessageFinalActivity" />
        <activity android:name=".PreferencesActivity" />
        <activity android:name=".ApiRestActivity" />
        <activity android:name=".ChooseAlarmActivity"></activity>
    </application>

</manifest>